<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <title>Admin Girişi</title>
    <style>
        :root { --bg-dark: #121212;
--bg-panel: #1e1e1e; --border-color: #333; --text-light: #e0e0e0; --text-muted: #aaa; --blue: #007bff; --red: #d9534f; --input-bg: #333; --input-border: #555;
}
        body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
display: grid; place-items: center; min-height: 100vh; background: var(--bg-dark); color: var(--text-light);
}
        .login-container { width: 350px;
}
        form { background: var(--bg-panel); padding: 30px; border-radius: 8px; border: 1px solid var(--border-color);
box-shadow: 0 4px 10px rgba(0,0,0,0.3); display: grid; gap: 15px; }
        h2 { margin: 0 0 15px 0;
text-align: center; }
        label { font-weight: 500; display: block; margin-bottom: 5px; color: var(--text-muted);
font-size: 0.9em; }
        input[type="password"] { padding: 12px; border-radius: 6px; border: 1px solid var(--input-border);
background: var(--input-bg); color: #f5f5f5; font-size: 1em; width: 100%; box-sizing: border-box;
}
        button { padding: 12px; border-radius: 6px; border: none; background: var(--blue); color: white;
font-size: 1em; font-weight: 500; cursor: pointer; transition: opacity 0.2s; }
        button:hover { opacity: 0.85;
}
        .message-container { position: fixed; top: 20px; right: 20px; z-index: 1000; width: 300px;
}
        .message { padding: 12px 18px; border-radius: 6px; margin-bottom: 15px; font-weight: 500;
opacity: 1; transition: opacity 0.5s ease-out; box-shadow: 0 4px 10px rgba(0,0,0,0.5);
}
        .message.fade-out { opacity: 0;
}
        .message.error { background: #dc3545; color: #fff;
}
    </style>
</head>
<body>
    
    <div class="message-container">
        <% if (errorMsg && errorMsg.length > 0) { %>
            <div class="message error" id="flash-message"><%= errorMsg %></div>
        <% } %>
    </div>

    <div class="login-container">
        <form action="/s-panel-a4x9/gizli-erisim-b7k2" method="POST">
            <h2>Admin Paneli Girişi</h2>
           
 <label for="password">Şifre:</label>
            <input type="password" id="password" name="password" required>
            <button type="submit">Giriş Yap</button>
        </form>
        </div>

    <script>
        const flashMessage = document.getElementById('flash-message');
if (flashMessage) {
            // Mesajın 3 saniye görünür kalmasını sağla
            setTimeout(() => {
                flashMessage.style.transition = 'opacity 0.5s ease-out'; // Fade-out animasyonunu ayarla
                flashMessage.style.opacity = '0'; // Fade-out'u başlat
            }, 3000); 
            
            // Animasyon bittikten sonra elementi DOM'dan kaldır
            setTimeout(() => {
                flashMessage.remove();
            }, 3600);
        }
    </script>
</body>
</html>